#!/usr/bin/with-contenv bash
# Wait for the desktop/X session to be ready before launching MT4
sleep 15
exec su abc -c "/usr/lib/wine/wine64 '/config/.wine/drive_c/Program Files (x86)/MetaTrader 4/terminal.exe'"
