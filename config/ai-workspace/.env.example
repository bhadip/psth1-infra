# Security: Copy this to .env and fill in real values. NEVER commit .env to git.

CODE_SERVER_PASSWORD=ChangeMeToSomethingStrong123!
SUDO_PASSWORD=ChangeMeToSomethingStrong123!

# Qwen API Configuration (DashScope or OpenRouter)
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_API_KEY=sk-your-actual-dashscope-api-key

# Optional: Cloudflare Tunnel Token (if managing tunnel via compose)
# TUNNEL_TOKEN=your-cloudflare-tunnel-token
